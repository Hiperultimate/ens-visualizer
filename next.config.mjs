/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Turbopack is the default in Next.js 16
  // viem/ensjs should work fine with Turbopack without additional config
}

export default nextConfig

